export type STATE_INFO_RESPONSE = {
    message: string,
}
export type STATE_INFO_REQUEST = {
    _id?: string,
    yearsExperience: number,
    projectsCompleted: number,
    technologies: number,
    happyClients: number,
}
